# delete-header-in-CSVfile

This scripts uses the os aand csv mmodules in python.
it checks if a file in a directory is a .csv file

for each .csv file, it deletes the first line and then writes the other part of the file back 
the files with their first lines deleted are then written to a new folder called "Headers removed" 
within the same directory